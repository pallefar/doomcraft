node_modules/
dist/
tools/.profile/
tools/.profile-*/        # capture-mc.mjs etc. create sibling profiles; these hold Cookies + Login Data
ref/**/video-*/
shots/
*.log
.DS_Store

# JsonFileStore writes here
server/.data/
.vercel

# Scratch probes written while verifying a finding. The captured test RUNS
# (.verify/runs) are evidence and are kept; these are not.
.verify/tmp/

# Gunfight capture scratch — screenshots kept only to debug a capture run.
tools/.gunfight-scratch/
